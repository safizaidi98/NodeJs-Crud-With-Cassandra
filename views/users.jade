extends layout

block content
    h1 Users
    a(href = '/adduser') Add User
    ul
        each user, i in users
            li 
                a(href = '/displayUser/#{user.username}') #{user.username}